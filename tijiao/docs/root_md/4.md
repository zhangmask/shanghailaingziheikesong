# 4号人员测试报告

## 测试环境

| 项目 | 内容 |
|------|------|
| Docker 镜像 | `mxcr.io/hpc-release/qiskit-aer-maca:0.17.2-3.7.0.6-ubuntu24.04-amd64` |
| GPU | 4× MetaX C500X 64GB |
| 远程服务器 | qiskit-metax-gpu-12 (10.9.202.91) |
| 测试脚本路径 | `/home/infra/qiskit_test_results/` |

---

## 一、量子算法测试

### 8.1 QFT（量子傅里叶变换）

| Qubits | 状态 | 详情 |
|--------|------|------|
| 8 | ❌ 失败 | "unknown instruction: QFT" - 需要 transpile 分解 |
| 12 | ❌ 失败 | 同上 |
| 16 | ❌ 失败 | 同上 |
| 20 | ❌ 失败 | 同上 |

**问题分析**: QFT 使用了 QFT 库函数，需要先 `transpile()` 分解为基础门。

### 8.2 Grover 搜索算法

| Qubits | 状态 | 详情 |
|--------|------|------|
| 8 | ✅ 通过 | 0.033s, target_prob=1.0 |
| 12 | ✅ 通过 | 12.048s, target_prob=1.0 |
| 16 | ✅ 通过 | 0.284s, target_prob=1.0 |

### 8.3 QPE（量子相位估计）

| Counting Qubits | 状态 | 详情 |
|-----------------|------|------|
| 8 | ❌ 失败 | "unknown instruction: IQFT" - 需要 transpile 分解 |
| 12 | ❌ 失败 | 同上 |
| 16 | ❌ 失败 | 同上 |

**问题分析**: QPE 使用了 QFT 库函数，需要先 `transpile()` 分解为基础门。

---

## 二、噪声模拟测试

### 5.1 振幅阻尼噪声模拟

| 状态 | 详情 |
|------|------|
| ❌ 失败 | "1 qubit QuantumError cannot be applied to 2 qubit instruction 'cx'" |

**问题分析**: 振幅阻尼使用了1量子比特错误应用于2量子比特cx门。需要使用 `error.tensor(error)` 创建2量子比特错误。

### 5.2 相位阻尼噪声模拟

| 状态 | 详情 |
|------|------|
| ❌ 失败 | 同上 |

**问题分析**: 同5.1，需要使用 `error.tensor(error)` 创建2量子比特错误。

### 5.3 去极化噪声模拟

| 状态 | 详情 |
|------|------|
| ✅ 通过 | p=0.01→0.995, p=0.05→0.977, p=0.1→0.954, p=0.3→0.848 |

### 5.4 混合噪声模型测试

| 状态 | 详情 |
|------|------|
| ✅ 通过 | bell_prob=0.988, time=0.003s |

---

## 三、电路特性测试

### 7.1 不同电路深度测试

| 深度 | 状态 | 详情 |
|------|------|------|
| 10 | ✅ 通过 | 0.107s, 522 states |
| 50 | ✅ 通过 | 0.018s, 522 states |
| 100 | ✅ 通过 | 0.033s, 524 states |
| 200 | ✅ 通过 | 0.067s, 513 states |

### 7.2 不同电路拓扑测试

| 拓扑 | 状态 | 详情 |
|------|------|------|
| 线性拓扑 | ✅ 通过 | 0.003s |
| 网格拓扑 | ✅ 通过 | 0.003s |
| 全连接拓扑 | ✅ 通过 | 0.006s |

### 7.3 随机电路测试

| Qubits | 状态 | 详情 |
|--------|------|------|
| 8 | ✅ 通过 | 0.003s, 104 states |
| 12 | ✅ 通过 | 0.004s, 444 states |
| 16 | ✅ 通过 | 0.012s, 912 states |

---

## 四、后端方法测试

### 6.1 superop GPU 测试

| 状态 | 详情 |
|------|------|
| ❌ 失败 | "No counts for experiment" - superop 不支持 measure |

**问题分析**: superop 方法不支持 `measure` 指令，需要使用 `save_statevector()` 替代。

### 6.2 tensor_network GPU 报错排查

| 状态 | 详情 |
|------|------|
| ❌ 失败 | "tensor_network only supports device=GPU"（已知问题） |

**问题分析**: 沐曦适配版 tensor_network 后端依赖 NV cutensor，无法在 MACA 上运行。错误信息自相矛盾是代码 bug。

### 6.3 automatic 方法自动选择

| 状态 | 详情 |
|------|------|
| ✅ 通过 | 0.018s, 248 states |

---

## 五、精度和一致性测试

### 8.1 GPU vs CPU 结果一致性验证

| 状态 | 详情 |
|------|------|
| ✅ 通过 | GPU=0.104s, CPU=0.007s, speedup=0.1x, max_prob_diff=0.001709 |

**结论**: GPU 和 CPU 结果高度一致，最大概率差异仅 0.17%。小电路(10q)CPU更快。

### 8.2 不同精度设置影响

| 方法 | 状态 | 详情 |
|------|------|------|
| statevector | ✅ 通过 | 0.005s |
| density_matrix | ✅ 通过 | 0.011s |

**结论**: 最大概率差异 0.001709，两种方法结果一致。

---

## 六、超算场景测试

### 9.1 多容器并行测试

| 状态 | 详情 |
|------|------|
| ✅ 通过 | GPU0=0.026s/250states, GPU1=0.002s/248states |

### 9.2 任务队列调度测试

| 状态 | 详情 |
|------|------|
| ✅ 通过 | total=0.011s, avg=0.002s, 5个任务 |

---

## 测试结果汇总

| 测试类别 | 通过/总数 | 通过率 |
|----------|----------|--------|
| 量子算法 (8.1-8.3) | 3/10 | 30% |
| 噪声模拟 (5.1-5.4) | 2/4 | 50% |
| 电路特性 (7.1-7.3) | 10/10 | 100% |
| 后端方法 (6.1-6.3) | 1/3 | 33% |
| 精度一致性 (8.1-8.2) | 2/2 | 100% |
| 超算场景 (9.1-9.2) | 2/2 | 100% |
| **总计** | **20/31** | **65%** |

---

## 主要发现

### 成功项

1. **Grover 搜索算法**: 8/12/16 qubits 全部通过，target_prob=1.0
2. **电路特性测试**: 全部通过，不同深度/拓扑/随机电路均正常
3. **精度一致性**: GPU vs CPU 最大概率差异仅 0.17%，结果高度一致
4. **超算场景**: 多GPU并行和任务队列均正常

### 已发现并修复的问题

1. **QFT/QPE**: 需要 `transpile()` 分解为基础门（已修复脚本）
2. **振幅/相位阻尼噪声**: 需要 `error.tensor(error)` 创建2量子比特错误（已修复脚本）
3. **superop**: 不支持 `measure` 指令，需用 `save_statevector()` 替代（已修复脚本）
4. **tensor_network**: 依赖 NV cutensor，沐曦适配版无法使用（已知问题）

### 待修复问题

- [ ] tensor_network 需沐曦适配 cuTensor
- [ ] QFT/QPE 的 transpile 修复已写入但未重新运行验证

---

## 优先级建议

| 优先级 | 任务 |
|--------|------|
| 🔴 高 | 8.1-8.3 量子算法测试（已修复 transpile 问题，需重新验证） |
| 🔴 高 | 5.1-5.4 噪声模拟测试（已修复 tensor 问题，需重新验证） |
| 🟡 中 | 7.1-7.3 电路特性测试 ✅ 已完成 |
| 🟡 中 | 6.1-6.3 后端方法测试（superop 已修复，tensor_network 待沐曦） |
| 🟡 中 | 8.1-8.2 精度一致性测试 ✅ 已完成 |
| 🟢 低 | 9.1-9.2 超算场景测试 ✅ 已完成 |
