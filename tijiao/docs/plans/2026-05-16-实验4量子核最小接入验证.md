# 实验4量子核最小接入验证 Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 以最小改动把量子核接入 LETKF 的观测加权项，并直接验证它在 `train` 和 `test_1` 上是否优于当前经典基线。

**Architecture:** 保持 `LETKF` 的预报步骤、集合传播和子空间求解主骨架不变，只把局部分析中的观测加权从固定等权替换为由量子核生成的对角权重。实验保留 `fixed`、`corr` 和 `quantum` 三组对照，统一输出 `RMSE/MAE` 与预测结果文件，用最短路径回答“接入后是否有效”。

**Tech Stack:** Python 3、numpy、qiskit 2.4、Markdown、远程 Ubuntu 环境

---

### Task 1: 固化实验4范围

**Files:**
- Read: `实验执行清单.md`
- Read: `气象海洋/shiyan/实验3_量子核结构实验.md`
- Modify: `task_plan.md`

**Step 1: 固定接入位置**

- 只改观测加权：
  - `R_i^{-1} -> diag(weights / obs_var)`
- 不改：
  - `RK4`
  - 集合传播
  - `sym_sqrt`
  - 子空间更新主骨架

**Step 2: 固定对照组**

- `fixed`
- `corr`
- `quantum`

### Task 2: 编写实验脚本

**Files:**
- Create: `气象海洋/shiyan/exp4_quantum_weighted_letkf.py`

**Step 1: 复用已有结构**

- 复用实验 2 的 `LETKF` 最小替换框架。
- 复用实验 3 的量子特征映射思路。

**Step 2: 实现量子权重**

- 用局部状态点和局部观测点的窗口特征生成量子核权重。
- 输出每个局部分析点的对角权重。

### Task 3: 远程运行验证

**Files:**
- Create remote: `/tmp/exp4_quantum_weighted_letkf.py`
- Create remote: `/home/infra/qda_competition/experiments/quantum_weighted_letkf/*`

**Step 1: 运行 train/test_1**

- 对三个方案都跑：
  - `fixed`
  - `corr`
  - `quantum`

**Step 2: 汇总指标**

- 保存：
  - `summary.json`
  - `summary.csv`
  - 各方案预测结果 CSV

### Task 4: 回写结论

**Files:**
- Create: `气象海洋/shiyan/实验4_量子核接入LETKF最小验证.md`
- Modify: `jindu.md`
- Modify: `progress.md`
- Modify: `findings.md`

**Step 1: 写结果表**

- 记录三组对照的：
  - `train RMSE/MAE`
  - `test_1 RMSE/MAE`

**Step 2: 写判断**

- 判断量子核版是否：
  - 优于 `fixed`
  - 优于 `corr`
  - 值得继续扩展
