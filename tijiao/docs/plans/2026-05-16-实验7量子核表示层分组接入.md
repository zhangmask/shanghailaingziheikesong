# 实验7量子核表示层分组接入 Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 将量子核从权重层前移到表示层/分组层，验证“量子核驱动局部观测分组 + 经典稳健更新”是否比直接量子权重接入更合理、更稳定。

**Architecture:** 复用 `exp5_quantum_mixed_weight_letkf.py` 的局部分析主骨架，但不再把量子核直接写入 `R^{-1}` 对角项。新脚本只让量子核负责局部观测的结构组织：先对局部观测做量子相似度分组，再在每个组内用经典稳健规则做更新摘要，最后合并为状态点的最终分析结果。

**Tech Stack:** Python 3、numpy、qiskit 2.4、Markdown、远程 Ubuntu 环境

---

### Task 1: 固化实验7范围和最小分组策略

**Files:**
- Read: `d:\Desktop\laingzimuxi\docs\plans\2026-05-16-实验7量子核表示层分组接入-design.md`
- Read: `d:\Desktop\laingzimuxi\气象海洋\shiyan\exp5_quantum_mixed_weight_letkf.py`
- Modify: `d:\Desktop\laingzimuxi\task_plan.md`

**Step 1: 固定实验7分组版本**

- 采用最小版本：
  - `quantum_grouped_update`
- 不同时做“原型观测压缩”版本，避免一次改太多

**Step 2: 固定分组规则**

- 先使用阈值分组：
  - 量子相似度高于阈值的观测优先归并
- 分组数控制在 `2-3` 组内
- 每组内部先用经典 `fixed` 或简单距离先验

### Task 2: 编写实验7脚本

**Files:**
- Create: `d:\Desktop\laingzimuxi\气象海洋\shiyan\exp7_quantum_grouped_letkf.py`

**Step 1: 复制实验5骨架**

- 复用：
  - 数据读取
  - `LETKF` 主骨架
  - `stable_inverse`
  - 输出 `summary.json/csv`
  - 输出 `best_test_prediction.csv` 和 `result.csv`

**Step 2: 新增局部观测量子分组**

- 在每个状态点 `i` 的 `local_obs_idx` 上：
  - 构造局部观测之间的量子核矩阵
  - 根据阈值或贪心方式分组

**Step 3: 新增组内经典更新摘要**

- 每个分组内部：
  - 保持经典稳健更新
  - 先用 `fixed` 组内等权
- 对每组得到：
  - 组内分析均值
  - 或组内分析增量

**Step 4: 新增跨组结果合并**

- 优先用简单合并方式：
  - 按组规模平均
  - 或按组内离散度加权
- 不在实验 7 第一版中加入复杂自适应合并

### Task 3: 本地静态检查

**Files:**
- Create: `d:\Desktop\laingzimuxi\气象海洋\shiyan\exp7_quantum_grouped_letkf.py`

**Step 1: 运行语法检查**

Run: `python -m py_compile "d:\Desktop\laingzimuxi\气象海洋\shiyan\exp7_quantum_grouped_letkf.py"`
Expected: 退出码 `0`

**Step 2: 检查诊断**

Run: `GetDiagnostics` for `exp7_quantum_grouped_letkf.py`
Expected: 无新增诊断错误

### Task 4: 远程运行实验7

**Files:**
- Create remote: `/tmp/exp7_quantum_grouped_letkf.py`
- Create remote: `/home/infra/qda_competition/experiments/quantum_grouped_letkf/*`

**Step 1: 上传并运行**

- 用稳定的 `base64` 方式上传脚本
- 运行 `fixed/corr/quantum_grouped_update`

**Step 2: 核对结果**

- 读取远程：
  - `summary.json`
  - `summary.csv`
- 提取：
  - 最优 `test_1 RMSE`
  - 分组数量统计
  - 典型局部状态点的分组规模信息

### Task 5: 回写实验7文档与进度

**Files:**
- Create: `d:\Desktop\laingzimuxi\气象海洋\shiyan\实验7_量子核表示层分组接入.md`
- Modify: `d:\Desktop\laingzimuxi\jindu.md`
- Modify: `d:\Desktop\laingzimuxi\progress.md`
- Modify: `d:\Desktop\laingzimuxi\findings.md`

**Step 1: 写结果表**

- 记录：
  - `fixed`
  - `corr`
  - `quantum_grouped_update`
- 写清 `train/test_1 RMSE/MAE`

**Step 2: 写结构性结果**

- 写清：
  - 分组数量统计
  - 典型局部分组结构
  - 表示层接入是否比权重层接入更合理

**Step 3: 写最终判断**

- 判断是否比实验 5 的直接权重层量子接入更稳
- 判断问题是否主要出在接入层位置
