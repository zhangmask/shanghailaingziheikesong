# 实验5量子核混合权重验证 Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 实现线性混合和距离混合两种量子核权重方案，直接验证它们在 `train` 和 `test_1` 上是否能优于当前 `fixed` 基线，并保持最终赛题输出链路兼容。

**Architecture:** 在已有 `LETKF` 最小替换实验框架上，新增两类混合权重构造：`linear_quantum_mix` 与 `distance_quantum_mix`。两类方案统一扫描小组 `lambda`，输出 `summary.json/csv`、预测结果文件和最优方案的赛题兼容结果文件，以最短路径回答“量子核混合后是否真正有效”。

**Tech Stack:** Python 3、numpy、qiskit 2.4、Markdown、远程 Ubuntu 环境

---

### Task 1: 固化实验5范围

**Files:**
- Read: `docs/plans/2026-05-16-实验5量子核混合权重验证-design.md`
- Read: `气象海洋/shiyan/实验4_量子核接入LETKF最小验证.md`
- Modify: `task_plan.md`

**Step 1: 固定方案和参数**

- 方案：
  - `linear_quantum_mix`
  - `distance_quantum_mix`
- `lambda` 扫描：
  - `0.1`
  - `0.2`
  - `0.3`
  - `0.5`

**Step 2: 固定对照与输出**

- 对照组：
  - `fixed`
  - `corr`
- 输出：
  - `train/test_1 RMSE/MAE`
  - 各 `lambda` 预测结果 CSV
  - 最优方案的赛题兼容结果文件

### Task 2: 编写实验5脚本

**Files:**
- Create: `气象海洋/shiyan/exp5_quantum_mixed_weight_letkf.py`

**Step 1: 复用已有实现**

- 复用 `exp4_quantum_weighted_letkf.py` 的：
  - 数据读取
  - `LETKF` 主骨架
  - 量子特征映射
  - 预测结果输出

**Step 2: 实现混合权重**

- 线性混合：
  - `w = (1 - lambda) + lambda * K_q`
- 距离混合：
  - `w = dist_weight * ((1 - lambda) + lambda * K_q)`
- 对权重做裁剪和归一化，避免实验 4 的强退化直接重现。

**Step 3: 输出最优方案**

- 根据 `test_1 RMSE` 选出最优混合方案。
- 额外生成：
  - `best_test_prediction.csv`
  - 如需要可直接映射到最终 `result.csv` 链路

### Task 3: 本地静态校验

**Files:**
- Create: `气象海洋/shiyan/exp5_quantum_mixed_weight_letkf.py`

**Step 1: 运行语法检查**

Run: `python -m py_compile "d:\Desktop\laingzimuxi\气象海洋\shiyan\exp5_quantum_mixed_weight_letkf.py"`
Expected: 退出码 `0`

**Step 2: 检查诊断**

Run: `GetDiagnostics` for `exp5_quantum_mixed_weight_letkf.py`
Expected: 无新增诊断错误

### Task 4: 远程运行实验

**Files:**
- Create remote: `/tmp/exp5_quantum_mixed_weight_letkf.py`
- Create remote: `/home/infra/qda_competition/experiments/quantum_mixed_weight_letkf/*`

**Step 1: 上传并运行**

- 用稳定的 `base64` 方式上传脚本。
- 在远程运行全部方案和全部 `lambda`。

**Step 2: 核对结果**

- 读取远程：
  - `summary.json`
  - `summary.csv`
- 提取：
  - 最优 `test_1 RMSE`
  - 对应方案与 `lambda`

### Task 5: 回写文档和进度

**Files:**
- Create: `气象海洋/shiyan/实验5_量子核混合权重验证.md`
- Modify: `jindu.md`
- Modify: `progress.md`
- Modify: `findings.md`

**Step 1: 写结果表**

- 记录：
  - `fixed`
  - `corr`
  - `linear_quantum_mix`
  - `distance_quantum_mix`
- 明确各 `lambda` 的 `train/test_1 RMSE/MAE`

**Step 2: 写最终判断**

- 判断是否有任一混合方案优于 `fixed`
- 判断量子核是否已对最终赛题方法产生正贡献
- 若无提升，明确下一步是否还值得继续该方向
