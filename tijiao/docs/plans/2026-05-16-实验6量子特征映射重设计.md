# 实验6量子特征映射重设计 Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 重做量子特征映射，使量子核更贴近观测可信度，并在最小改动下验证它是否优于实验 5 的旧量子混合版本。

**Architecture:** 复用 `exp5_quantum_mixed_weight_letkf.py` 的 `LETKF` 主骨架、数据读取、输出和赛题兼容链路，只替换量子特征构造方式。新版本不再用“状态窗口 vs 观测窗口”的直接数值相似度，而是编码创新幅值、集合离散度、状态-观测耦合强度和距离先验，并仅在小 `lambda` 区间内做线性弱混合验证。

**Tech Stack:** Python 3、numpy、qiskit 2.4、Markdown、远程 Ubuntu 环境

---

### Task 1: 固化实验6范围和对照

**Files:**
- Read: `d:\Desktop\laingzimuxi\docs\plans\2026-05-16-实验6量子特征映射重设计-design.md`
- Read: `d:\Desktop\laingzimuxi\气象海洋\shiyan\exp5_quantum_mixed_weight_letkf.py`
- Modify: `d:\Desktop\laingzimuxi\task_plan.md`

**Step 1: 固定实验6输入和输出**

- 对照组：
  - `fixed`
  - `corr`
  - `linear_quantum_mix` 的实验 5 最优结果
  - `credibility_quantum_mix`
- `lambda` 扫描：
  - `0.02`
  - `0.05`
  - `0.08`
  - `0.10`
  - `0.12`
  - `0.15`

**Step 2: 固定验证目标**

- 验证新量子特征映射是否：
  - 比实验 5 更稳定
  - 比实验 5 的最佳混合更接近或优于 `fixed`
  - 更符合“观测可信度”而不是“窗口相似度”

### Task 2: 编写实验6脚本

**Files:**
- Create: `d:\Desktop\laingzimuxi\气象海洋\shiyan\exp6_quantum_credibility_features.py`

**Step 1: 复制实验5骨架**

- 复用：
  - 数据读取
  - `LETKF` 主骨架
  - `stable_inverse`
  - 输出 `summary.json/csv`
  - 输出 `best_test_prediction.csv` 和 `result.csv`

**Step 2: 新增可信度特征提取**

- 为每个局部状态点 `i` 与局部观测点 `j` 提取：
  - 归一化创新幅值
  - 归一化集合离散度
  - 状态-观测耦合强度
  - 归一化环距离

**Step 3: 新增量子特征映射与量子核构造**

- 保持浅层量子线路规模不变，优先沿用 `N_QUBITS = 4`
- 用新的可信度特征替换旧的局部窗口数值特征
- 输出新的 `quantum_lookup`

**Step 4: 只实现弱线性混合**

- 用：
  - `w = (1 - lambda) + lambda * K_q_cred`
- 不在实验 6 中同时引入距离混合，避免变量过多

### Task 3: 本地静态检查

**Files:**
- Create: `d:\Desktop\laingzimuxi\气象海洋\shiyan\exp6_quantum_credibility_features.py`

**Step 1: 运行语法检查**

Run: `python -m py_compile "d:\Desktop\laingzimuxi\气象海洋\shiyan\exp6_quantum_credibility_features.py"`
Expected: 退出码 `0`

**Step 2: 检查诊断**

Run: `GetDiagnostics` for `exp6_quantum_credibility_features.py`
Expected: 无新增诊断错误

### Task 4: 远程运行实验6

**Files:**
- Create remote: `/tmp/exp6_quantum_credibility_features.py`
- Create remote: `/home/infra/qda_competition/experiments/quantum_credibility_features/*`

**Step 1: 上传并运行**

- 用稳定的 `base64` 方式上传脚本
- 扫描全部 `lambda`

**Step 2: 核对结果**

- 读取远程：
  - `summary.json`
  - `summary.csv`
- 提取：
  - 最优 `test_1 RMSE`
  - 对应 `lambda`
  - 与实验 5 最优量子混合的差异

### Task 5: 回写实验6文档与进度

**Files:**
- Create: `d:\Desktop\laingzimuxi\气象海洋\shiyan\实验6_量子特征映射重设计.md`
- Modify: `d:\Desktop\laingzimuxi\jindu.md`
- Modify: `d:\Desktop\laingzimuxi\progress.md`
- Modify: `d:\Desktop\laingzimuxi\findings.md`

**Step 1: 写结果表**

- 记录：
  - `fixed`
  - `corr`
  - `credibility_quantum_mix`
- 明确各 `lambda` 的 `train/test_1 RMSE/MAE`

**Step 2: 写最终判断**

- 判断是否优于实验 5 的最佳量子混合
- 判断是否更稳定
- 判断问题是否主要出在原量子特征定义
