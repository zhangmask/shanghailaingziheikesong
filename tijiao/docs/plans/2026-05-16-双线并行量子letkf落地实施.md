# 双线并行量子 LETKF 落地实施 Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 新增一条短期稳妥线 `exp8`、一条中期创新线 `exp9` 和统一对比脚本，把量子信息从“直接权重替代”推进到“结构调制”和“表示层分组”两条可执行实验线。

**Architecture:** 复用当前 `exp5/exp7` 的 `LETKF` 骨架、数据读取、数值稳定函数和赛题输出链路；`exp8` 基于 `exp5` 做局部 `block` 级量子弱调制，`exp9` 基于 `exp7` 做观测分组/降阶；最后用单独脚本汇总 `fixed/corr/exp5/exp8/exp9` 的结果并回写实验文档与 `jindu.md`。

**Tech Stack:** Python 3、NumPy、Qiskit 2.4、Markdown、远程 Ubuntu 运行环境

---

### Task 1: 固化双线并行实验输入与对照

**Files:**
- Read: `d:\Desktop\laingzimuxi\docs\plans\2026-05-16-双线并行量子letkf落地实施-design.md`
- Read: `d:\Desktop\laingzimuxi\气象海洋\shiyan\exp5_quantum_mixed_weight_letkf.py`
- Read: `d:\Desktop\laingzimuxi\气象海洋\shiyan\exp7_quantum_grouped_letkf.py`
- Modify: `d:\Desktop\laingzimuxi\task_plan.md`

**Step 1: 固定短期线和中期线的目标**

- 短期线：
  - 基线：`exp5`
  - 新脚本：`exp8_quantum_block_mix_letkf.py`
  - 目标：比 `exp5` 最优量子混合更稳或更接近 `fixed`
- 中期线：
  - 基线：`exp7`
  - 新脚本：`exp9_quantum_reduced_group_letkf.py`
  - 目标：验证表示层分组是否比权重层直接接入更合理

**Step 2: 固定统一对照组**

- 对照组固定为：
  - `fixed`
  - `corr`
  - `exp5 linear_quantum_mix, lambda = 0.1`
  - `exp8` 最优版本
  - `exp9` 最优版本

**Step 3: 固定第一轮扫描范围**

- `exp8` 扫描：
  - `lambda = 0.02, 0.05, 0.08, 0.10`
- `exp9` 扫描：
  - `group_threshold = 0.85, 0.90, 0.95`
  - `max_groups = 2, 3`

### Task 2: 创建 exp8 脚本骨架

**Files:**
- Create: `d:\Desktop\laingzimuxi\气象海洋\shiyan\exp8_quantum_block_mix_letkf.py`
- Read: `d:\Desktop\laingzimuxi\气象海洋\shiyan\exp5_quantum_mixed_weight_letkf.py`

**Step 1: 复制 exp5 的稳定骨架**

- 保留：
  - `load_competition_csv`
  - `rk4_step`
  - `sym_sqrt`
  - `stable_inverse`
  - 结果导出与 `summary.json/csv`

**Step 2: 固定 exp8 的方法集合**

- 第一版仅保留：
  - `fixed`
  - `corr`
  - `block_quantum_mix`

**Step 3: 固定 exp8 的参数常量**

- 新增：
  - `BLOCK_SIZE = 4`
  - `LAMBDAS = [0.02, 0.05, 0.08, 0.10]`

### Task 3: 在 exp8 中实现 block 级量子结构信号

**Files:**
- Modify: `d:\Desktop\laingzimuxi\气象海洋\shiyan\exp8_quantum_block_mix_letkf.py`

**Step 1: 新增 block 特征构造函数**

- 以局部观测块为单位提取：
  - block 均值
  - block 方差或离散度
  - block innovation 幅值
  - block 与状态点距离先验

**Step 2: 新增 block 量子映射函数**

- 复用现有 `build_feature_map`
- 输入改为 block 级四元特征
- 输出 block 相似度或 block 可信度信号

**Step 3: 新增 block 信号压回观测点层的函数**

- 将 block 结构信号映射回局部观测点
- 第一版规则固定为：
  - 同 block 内共享同一量子调制系数

### Task 4: 在 exp8 中接入 block_quantum_mix 更新

**Files:**
- Modify: `d:\Desktop\laingzimuxi\气象海洋\shiyan\exp8_quantum_block_mix_letkf.py`

**Step 1: 新增 `compute_block_quantum_weights()`**

- 公式固定为：
  - `base_mix = (1 - lambda) + lambda * block_signal`
  - 与经典先验相乘后再归一化

**Step 2: 先只保留一种经典先验**

- 第一版使用：
  - `distance_prior * base_mix`
- 不同时实现 `corr_prior`，避免变量过多

**Step 3: 将方法接入 `letkf_update_weighted()`**

- 仅在 `method == "block_quantum_mix"` 时启用 block 信号
- 其他方法继续保持原始逻辑

### Task 5: 本地静态检查 exp8

**Files:**
- Create: `d:\Desktop\laingzimuxi\气象海洋\shiyan\exp8_quantum_block_mix_letkf.py`

**Step 1: 运行语法检查**

Run: `python -m py_compile "d:\Desktop\laingzimuxi\气象海洋\shiyan\exp8_quantum_block_mix_letkf.py"`
Expected: 退出码 `0`

**Step 2: 检查诊断**

Run: `GetDiagnostics` for `exp8_quantum_block_mix_letkf.py`
Expected: 无新增诊断错误

### Task 6: 远程运行 exp8

**Files:**
- Create remote: `/tmp/exp8_quantum_block_mix_letkf.py`
- Create remote: `/home/infra/qda_competition/experiments/quantum_block_mix_letkf/*`

**Step 1: 上传并运行**

- 用稳定的 `base64` 方式上传脚本
- 运行：
  - `fixed`
  - `corr`
  - `block_quantum_mix`

**Step 2: 核对结果**

- 读取远程：
  - `summary.json`
  - `summary.csv`
- 提取：
  - 最优 `lambda`
  - 最优 `test_1 RMSE`
  - 与 `exp5 linear_quantum_mix, lambda = 0.1` 的差异

### Task 7: 创建 exp9 脚本骨架

**Files:**
- Create: `d:\Desktop\laingzimuxi\气象海洋\shiyan\exp9_quantum_reduced_group_letkf.py`
- Read: `d:\Desktop\laingzimuxi\气象海洋\shiyan\exp7_quantum_grouped_letkf.py`

**Step 1: 复制 exp7 的稳定骨架**

- 保留：
  - 全部共享数值函数
  - 局部分组统计输出
  - `summary.json/csv`
  - `best_test_prediction.csv`
  - `result.csv`

**Step 2: 固定 exp9 的方法集合**

- 第一版仅保留：
  - `fixed`
  - `corr`
  - `quantum_reduced_group`

**Step 3: 固定 exp9 的分组参数**

- 新增扫描：
  - `GROUP_THRESHOLDS = [0.85, 0.90, 0.95]`
  - `MAX_GROUPS_LIST = [2, 3]`

### Task 8: 在 exp9 中实现降阶/分组逻辑

**Files:**
- Modify: `d:\Desktop\laingzimuxi\气象海洋\shiyan\exp9_quantum_reduced_group_letkf.py`

**Step 1: 保留 `obs-obs` 量子相似度矩阵**

- 继续复用 `build_obs_similarity`
- 不再计算 `state-obs` 点对量子权重

**Step 2: 新增最小降阶规则**

- 对每个局部分组：
  - 保留组内全部观测
  - 同时计算组代表摘要
- 第一版不直接丢弃组内观测，只新增分组结构

**Step 3: 固定组内经典更新**

- 组内统一使用：
  - `fixed`
- 不同时引入组内距离先验

**Step 4: 固定跨组合并规则**

- 第一版只实现：
  - 按组规模平均
- 额外保留接口，后续再加按离散度加权

### Task 9: 本地静态检查 exp9

**Files:**
- Create: `d:\Desktop\laingzimuxi\气象海洋\shiyan\exp9_quantum_reduced_group_letkf.py`

**Step 1: 运行语法检查**

Run: `python -m py_compile "d:\Desktop\laingzimuxi\气象海洋\shiyan\exp9_quantum_reduced_group_letkf.py"`
Expected: 退出码 `0`

**Step 2: 检查诊断**

Run: `GetDiagnostics` for `exp9_quantum_reduced_group_letkf.py`
Expected: 无新增诊断错误

### Task 10: 远程运行 exp9

**Files:**
- Create remote: `/tmp/exp9_quantum_reduced_group_letkf.py`
- Create remote: `/home/infra/qda_competition/experiments/quantum_reduced_group_letkf/*`

**Step 1: 上传并运行**

- 用稳定的 `base64` 方式上传脚本
- 运行：
  - `fixed`
  - `corr`
  - `quantum_reduced_group`

**Step 2: 核对结构结果**

- 读取远程：
  - `summary.json`
  - `summary.csv`
- 提取：
  - 最优分组参数
  - `avg_group_count`
  - `avg_group_size`
  - `max_group_count`

**Step 3: 核对误差结果**

- 提取：
  - 最优 `test_1 RMSE`
  - 与 `exp7` 当前结果的差异
  - 与 `exp5` 最佳量子版本的差异

### Task 11: 创建统一比较脚本

**Files:**
- Create: `d:\Desktop\laingzimuxi\气象海洋\shiyan\compare_quantum_variants.py`

**Step 1: 固定输入来源**

- 读取：
  - `exp5` 输出目录
  - `exp8` 输出目录
  - `exp9` 输出目录

**Step 2: 统一指标字段**

- 输出统一表格列：
  - `variant`
  - `train_rmse`
  - `train_mae`
  - `test_1_rmse`
  - `test_1_mae`
  - `delta_vs_fixed`

**Step 3: 导出对比结果**

- 生成：
  - `comparison_summary.csv`
  - `comparison_summary.md`

### Task 12: 本地检查 compare 脚本

**Files:**
- Create: `d:\Desktop\laingzimuxi\气象海洋\shiyan\compare_quantum_variants.py`

**Step 1: 运行语法检查**

Run: `python -m py_compile "d:\Desktop\laingzimuxi\气象海洋\shiyan\compare_quantum_variants.py"`
Expected: 退出码 `0`

**Step 2: 检查诊断**

Run: `GetDiagnostics` for `compare_quantum_variants.py`
Expected: 无新增诊断错误

### Task 13: 回写实验文档与进度

**Files:**
- Create: `d:\Desktop\laingzimuxi\气象海洋\shiyan\实验8_量子block弱混合验证.md`
- Create: `d:\Desktop\laingzimuxi\气象海洋\shiyan\实验9_量子降阶分组验证.md`
- Modify: `d:\Desktop\laingzimuxi\jindu.md`
- Modify: `d:\Desktop\laingzimuxi\progress.md`
- Modify: `d:\Desktop\laingzimuxi\findings.md`

**Step 1: 写 exp8 结果**

- 记录：
  - 各 `lambda` 的 `train/test_1 RMSE/MAE`
  - 是否比 `exp5` 更接近 `fixed`

**Step 2: 写 exp9 结果**

- 记录：
  - 各分组参数的 `train/test_1 RMSE/MAE`
  - 分组数量和组规模统计
  - 是否比权重层接入更有解释力

**Step 3: 写总结判断**

- 明确：
  - 短期可交付线是否仍以 `fixed` 为主
  - 中期创新线是否支持继续推进“结构组织层”
  - 下一步是否允许做 `可信度 + 分组` 交叉实验

### Task 14: 结束前统一核验

**Files:**
- Read: `d:\Desktop\laingzimuxi\气象海洋\shiyan\exp8_quantum_block_mix_letkf.py`
- Read: `d:\Desktop\laingzimuxi\气象海洋\shiyan\exp9_quantum_reduced_group_letkf.py`
- Read: `d:\Desktop\laingzimuxi\气象海洋\shiyan\compare_quantum_variants.py`
- Read: `d:\Desktop\laingzimuxi\jindu.md`

**Step 1: 统一检查输出链路**

- 确认三份脚本都能正确输出：
  - `summary.json/csv`
  - `best_test_prediction.csv`
  - `result.csv` 或对比汇总

**Step 2: 统一检查结果口径**

- 确认所有指标字段含义一致
- 确认 `fixed` 仍作为最终统一参照

**Step 3: 整理最终执行建议**

- 若 `exp8` 明显更稳：
  - 优先继续短期线
- 若 `exp9` 结构证据明显更强：
  - 优先继续表示层路线
- 若两条线都无积极信号：
  - 暂停扩大实现复杂度
