# 报告修改问题清单

用途：记录当前 `paper_cvpr_report.tex` 及其章节文件需要完成的关键修改项。每完成一项后，将对应复选框从 `[ ]` 改为 `[x]`。

当前状态：未完成

---

## A. 核心方法数学定义

- [ ] 明确定义 `J` 的数学含义
  - 需要写清：`J` 是局地集合扰动方向上的什么量，来源于哪些局地模式或相似度信息。
  - 需要补到：`03_quantum_algorithm.tex`，必要时在 `02_modeling.tex` 先定义符号。
  - 验收标准：读者能从正文直接找到 `J` 的定义，而不是只看到“弱 J 修正”的口号。

- [ ] 给出 `J` 修正项的明确公式
  - 需要写清：修正项如何构造、加到哪一个量上、系数如何控制强度。
  - 需要补到：`03_quantum_algorithm.tex`。
  - 验收标准：正文中出现完整公式，而不是只用自然语言描述。

- [ ] 给出门控条件的明确表达
  - 需要写清：什么叫“局地耦合可信”，门控变量或阈值如何定义。
  - 需要补到：`03_quantum_algorithm.tex`。
  - 验收标准：读者可以判断在哪些情况下修正会触发，哪些情况下不会触发。

- [ ] 给出最终更新公式
  - 需要写清：量子弱融合后的局地权重、门控 `J` 修正与 LETKF 更新如何合成到最终分析公式中。
  - 需要补到：`02_modeling.tex` 与 `03_quantum_algorithm.tex`。
  - 验收标准：方法部分形成“基线公式 -> 替换项/新增项 -> 最终公式”的完整闭环。

---

## B. 实验对比与消融

- [ ] 增加系统性消融表
  - 需要包含：`fixed`、`corr`、更接近当前方法的中间版本、最终量子弱融合版本。
  - 需要补到：`05_results.tex` 或新增表格文件后 `\input`。
  - 验收标准：读者能看到“从失败到修正”的完整路线，而不是只看到三行最终结果。

- [ ] 把已经做过的关键结果真正放进正文
  - 需要包含：失败版本、过强修正版、弱融合版本、必要时加入 train/test 对比。
  - 需要补到：`05_results.tex`、`07_comparison.tex`。
  - 验收标准：正文不再像“从很多实验里挑一个最好看的结果摆上来”。

- [ ] 增加更接近方法版本之间的逐项比较
  - 需要包含：仅 `rho` 弱融合、`rho + J` 弱修正、门控版本、可能的 `adaptive B` 近邻方案。
  - 需要补到：`07_comparison.tex`。
  - 验收标准：比较对象不是过于粗糙的基线，而是方法内部相邻版本。

---

## C. 相关工作与创新性约束

- [ ] 增加 related work 小节
  - 需要覆盖：经典 LETKF/局地加权、量子核/量子特征映射、量子调参思路、量子滤波/同化相关方向。
  - 需要放置：建议新增 `paper_sections/00a_related_work.tex` 或并入 `01_problem_analysis.tex` 前半部分。
  - 验收标准：创新点不再是“自封”，而是建立在已有路线区分之上。

- [ ] 重写创新点表述
  - 需要把“作用位点创新、表示层创新、融合机制创新”逐条改成“相对于什么已有方法的差异”。
  - 需要补到：`06_innovations.tex`。
  - 验收标准：每个创新点都包含“已有做法 / 本文做法 / 差异与意义”三部分。

- [ ] 降低结论强度
  - 需要把“可信路径”“方法论结论”改写为“当前实验设定下的证据支持”。
  - 需要补到：摘要、`06_innovations.tex`、`08_summary.tex`。
  - 验收标准：结论与证据规模相匹配，避免过度外推。

---

## D. 实验设置透明度

- [ ] 补全实验设置
  - 需要写清：ensemble size、inflation、localization radius、窗口长度、融合系数搜索范围、最优参数、随机种子、train/validation/test 划分。
  - 需要补到：建议在 `05_results.tex` 前增设“实验设置”小节，或新增 `05a_experiment_setup.tex`。
  - 验收标准：读者可以复现实验，不再需要猜配置。

- [ ] 解释表 1 中“最优”的来源
  - 需要写清：从哪些配置中筛选、按什么指标选出、是否存在验证集筛选。
  - 需要补到：`05_results.tex`。
  - 验收标准：表中“最优”不再像随意挑选，而是有透明流程。

---

## E. 图表分析定量化

- [ ] 给现有图补具体数字
  - 需要补充：尾部误差下降比例、优于基线的时间步占比、改善维度数量、残差方差变化等。
  - 需要补到：`05_results.tex`、`07_comparison.tex`。
  - 验收标准：图注和正文不再只说“说明了/表明了”，而是给出定量证据。

- [ ] 每张图都绑定一个明确问题
  - 需要把每张图改写成回答某个问题，如“是否稳定”“是否局地有效”“是否不是单纯贴观测”。
  - 需要补到：对应章节逐图分析段落。
  - 验收标准：删掉任意一张图时，能明确知道它原本承担哪条证据链。

---

## F. 论文风格与结构收紧

- [ ] 收紧第 1 节的项目汇报口吻
  - 需要把“我们如何一步步试错”的口语叙述压缩成更学术的“问题、挑战、方法选择理由”。
  - 需要补到：`01_problem_analysis.tex`。
  - 验收标准：保留思考过程，但整体更像论文而不是答辩记录。

- [ ] 加强模型层
  - 需要明确：原始 LETKF 中局地观测权重如何进入公式，本文替换或修改的是哪一部分。
  - 需要补到：`02_modeling.tex`。
  - 验收标准：模型层能够清楚说明“本文相对基线改动了哪里”。

- [ ] 去除重复表述
  - 需要重点检查：`弱修正`、`几何提示`、`不是替代而是增强` 等重复句式。
  - 需要补到：全篇。
  - 验收标准：同一观点不在多个章节机械重复。

- [ ] 把第 7 节改成证据驱动
  - 需要加入表格、定量图和明确比较指标，不再主要依赖主观判断词。
  - 需要补到：`07_comparison.tex`。
  - 验收标准：比较章节里每个判断都有数字或图支撑。

- [ ] 评估标题是否过大
  - 备选方向：
    - `Lorenz96 数据同化中的量子弱融合实验研究`
    - `面向 Lorenz96 数据同化的量子弱融合受控实验报告`
    - `基于局地量子相似度的 Lorenz96 数据同化增强方法`
  - 需要补到：主文件 `paper_cvpr_report.tex`。
  - 验收标准：标题与证据规模相称。

---

## G. 推荐执行顺序

- [ ] 第一步：补方法定义与最终公式
- [ ] 第二步：补实验设置与系统性消融表
- [ ] 第三步：补 related work 与创新点重写
- [ ] 第四步：把图分析全部定量化
- [ ] 第五步：统一收紧摘要、结论、标题和措辞强度

---

## H. 完成记录

- [ ] 已建立问题清单文档
- [ ] 已开始按清单逐项修改正文
- [ ] 所有关键问题已关闭
