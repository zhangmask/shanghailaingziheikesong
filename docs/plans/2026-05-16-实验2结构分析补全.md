# 实验2结构分析补全 Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 补齐实验 2 的结构性输出，生成典型时间步的观测权重可视化与统计证据，并同步更新实验文档和进度文档。

**Architecture:** 在本地用最小复现实验脚本重建 `LETKF` 的经典代理核更新过程，只保留与实验 2 相关的权重计算和局部分析部分。先验证能复现既有 RMSE 量级，再导出典型时刻的权重向量、统计摘要和图像，最后把结构性结论写回文档。

**Tech Stack:** Python 3、numpy、csv、matplotlib（若可用）、Markdown

---

### Task 1: 明确补全范围

**Files:**
- Read: `实验执行清单.md`
- Read: `气象海洋/shiyan/实验2_经典代理核实验.md`
- Modify: `task_plan.md`

**Step 1: 核对实验 2 缺口**

- 检查执行清单中实验 2 的产物要求。
- 对照现有实验 2 文档，确认缺的是结构图和权重曲线，而不是主结果表。

**Step 2: 固化补全目标**

- 明确只补以下内容：
  - 典型时间步权重热图
  - 典型状态点观测权重曲线
  - 权重分布摘要统计

### Task 2: 复现实验 2 权重计算

**Files:**
- Create: `气象海洋/shiyan/exp2_structure_analysis.py`
- Read: `气象海洋/气象海洋/小规模测试/lorenz96_train.csv`

**Step 1: 写最小复现脚本**

- 实现：
  - CSV 读取
  - Lorenz96 `rk4_step`
  - `fixed/corr/innov/hybrid` 权重
  - 局部分析时的权重提取

**Step 2: 先做数值自检**

- 在少量时间步上运行，确认输出维度、权重范围、无 NaN 崩溃。

### Task 3: 导出结构性产物

**Files:**
- Modify: `气象海洋/shiyan/exp2_structure_analysis.py`
- Create: `气象海洋/shiyan/实验2_权重结构分析_*.png`
- Create: `气象海洋/shiyan/实验2_权重结构分析_summary.md`

**Step 1: 选定代表时刻**

- 选早期、中期、后期各一个时间步。
- 对固定的局部状态点导出 4 类核的权重向量。

**Step 2: 生成图与摘要**

- 若 `matplotlib` 可用，生成热图与曲线图。
- 同时输出文本摘要，保证无图环境下也能使用。

### Task 4: 回写文档

**Files:**
- Modify: `气象海洋/shiyan/实验2_经典代理核实验.md`
- Modify: `jindu.md`
- Modify: `progress.md`
- Modify: `findings.md`

**Step 1: 补写结构性结论**

- 写清：
  - 固定核的权重为何更稳
  - `corr` 为何“有信息但不够好”
  - `innov/hybrid` 为何容易过抑制

**Step 2: 记录后续决策**

- 明确实验 2 现在可视为“结论 + 结构证据”完整。
- 把下一步建议收敛到“是否开始实验 3”。
