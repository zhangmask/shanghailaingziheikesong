# 量子局地化膨胀鲁棒三线 LETKF 实验 Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 在不干扰当前远端 `exp9/10/11/12` 运行的前提下，新增三条独立实验线：量子局地化核、量子膨胀项和鲁棒创新度量，并统一采用“本地写脚本、静默上传、最后统一下载结果”的流程。

**Architecture:** 新实验全部单独命名为 `exp13/14/15`，各自拥有独立本地脚本、独立远端输出目录和独立本地结果抓取目录；实现上复用当前 `exp5/exp10/exp11/exp12` 的 `Lorenz96 + LETKF` 骨架与量子特征映射，但每条线只改一个新的公式层位置，避免与已运行实验发生目录、进程或结果覆盖冲突。

**Tech Stack:** Python 3、NumPy、Qiskit 2.4、Markdown、远端 Ubuntu 运行环境

---

### Task 1: 固化不干扰约束与目录规则

**Files:**
- Read: `d:\Desktop\laingzimuxi\气象海洋\shiyan\exp10_quantum_structured_R_letkf.py`
- Read: `d:\Desktop\laingzimuxi\气象海洋\shiyan\exp11_quantum_S_operator_letkf.py`
- Read: `d:\Desktop\laingzimuxi\气象海洋\shiyan\exp12_quantum_reduced_analysis_space_letkf.py`

**Step 1: 固定新实验脚本命名**

- Create:
  - `d:\Desktop\laingzimuxi\气象海洋\shiyan\exp13_quantum_localization_letkf.py`
  - `d:\Desktop\laingzimuxi\气象海洋\shiyan\exp14_quantum_inflation_letkf.py`
  - `d:\Desktop\laingzimuxi\气象海洋\shiyan\exp15_quantum_robust_innovation_letkf.py`

**Step 2: 固定远端输出目录**

- `exp13`:
  - `/home/infra/qda_competition/experiments/quantum_localization_letkf`
- `exp14`:
  - `/home/infra/qda_competition/experiments/quantum_inflation_letkf`
- `exp15`:
  - `/home/infra/qda_competition/experiments/quantum_robust_innovation_letkf`

**Step 3: 固定执行原则**

- 不复用 `exp9/10/11/12` 的远端输出目录
- 不 `pkill` 当前已运行的其他实验脚本
- 新实验一律静默上传与静默抓取

### Task 2: 设计并实现 exp13 量子局地化核实验

**Files:**
- Create: `d:\Desktop\laingzimuxi\气象海洋\shiyan\exp13_quantum_localization_letkf.py`

**Step 1: 复用稳定骨架**

- 从 `exp10` 或 `exp5` 复制：
  - `load_competition_csv`
  - `rk4_step`
  - `stable_inverse`
  - `sym_sqrt`
  - 结果导出链路

**Step 2: 固定量子局地化公式**

- 经典：
  - `rho_classical(i,j) = rho(distance(i,j))`
- 量子版第一轮只做乘性混合：
  - `rho_q(i,j) = rho_classical(i,j) * ((1 - lambda) + lambda * K_q(i,j))`

**Step 3: 接入位置**

- 用 `rho_q` 改局部观测参与度或局部权重先验
- 不同时再改 `R`、`S` 或 `inflation`

**Step 4: 第一轮扫描**

- `LAMBDAS = [0.02, 0.05, 0.10]`

### Task 3: 设计并实现 exp14 量子膨胀项实验

**Files:**
- Create: `d:\Desktop\laingzimuxi\气象海洋\shiyan\exp14_quantum_inflation_letkf.py`

**Step 1: 保留经典分析矩阵结构**

- `A = (k - 1)/infl * I + ...`

**Step 2: 构造局部量子膨胀**

- 第一轮只做局部标量膨胀：
  - `infl_q(i) = infl_base * ((1 - lambda) + lambda * g_q(i))`
- `g_q(i)` 由量子结构、局部 innovation 幅值、局部 spread 摘要构成

**Step 3: 接入位置**

- 仅替换局部 `(k - 1)/infl`
- 不同时改权重和局地化

**Step 4: 第一轮扫描**

- `LAMBDAS = [0.02, 0.05, 0.10]`

### Task 4: 设计并实现 exp15 鲁棒创新度量实验

**Files:**
- Create: `d:\Desktop\laingzimuxi\气象海洋\shiyan\exp15_quantum_robust_innovation_letkf.py`

**Step 1: 保留经典二次型基线**

- 继续保留：
  - `fixed`
  - `corr`

**Step 2: 第一轮采用最小鲁棒形式**

- 不直接做复杂非凸损失
- 先用量子结构调节的加权 Huber 近似：
  - `rho_q(e_j)` 的阈值由局部量子结构给出

**Step 3: 接入位置**

- 只改 innovation 度量
- 不同时改 `R`、`S`、`inflation`

**Step 4: 第一轮扫描**

- 固定一组保守阈值
- 只测试最小可运行版，先确认稳定性

### Task 5: 统一本地静态检查

**Files:**
- Create:
  - `d:\Desktop\laingzimuxi\气象海洋\shiyan\exp13_quantum_localization_letkf.py`
  - `d:\Desktop\laingzimuxi\气象海洋\shiyan\exp14_quantum_inflation_letkf.py`
  - `d:\Desktop\laingzimuxi\气象海洋\shiyan\exp15_quantum_robust_innovation_letkf.py`

**Step 1: 逐个运行 `py_compile`**

Run:
- `python -m py_compile "d:\Desktop\laingzimuxi\气象海洋\shiyan\exp13_quantum_localization_letkf.py"`
- `python -m py_compile "d:\Desktop\laingzimuxi\气象海洋\shiyan\exp14_quantum_inflation_letkf.py"`
- `python -m py_compile "d:\Desktop\laingzimuxi\气象海洋\shiyan\exp15_quantum_robust_innovation_letkf.py"`

Expected:
- 全部退出码 `0`

**Step 2: 检查 diagnostics**

- 确认无新增语法或类型级错误

### Task 6: 统一静默上传与远端启动

**Files:**
- Modify local status files under:
  - `d:\Desktop\laingzimuxi\气象海洋\shiyan\exp13_quantum_localization_outputs\`
  - `d:\Desktop\laingzimuxi\气象海洋\shiyan\exp14_quantum_inflation_outputs\`
  - `d:\Desktop\laingzimuxi\气象海洋\shiyan\exp15_quantum_robust_innovation_outputs\`

**Step 1: exp13 静默启动**

- 上传脚本
- 只写本地 `launch_status.txt`
- 不在终端打印远端结果

**Step 2: exp14 静默启动**

- 与 `exp13` 相同

**Step 3: exp15 静默启动**

- 与 `exp13` 相同

### Task 7: 统一结果验收准备

**Files:**
- Create local fetch files for each experiment

**Step 1: 只抓取结果文件**

- 目标文件：
  - `summary.json`
  - `summary.csv`
  - `result.csv`

**Step 2: 若未完成则只记录状态**

- `NO_SUMMARY_YET`
- 不读取长日志

**Step 3: 等现有实验一起统一验收**

- `exp9/10/11/12/13/14/15` 最后统一下载与对比
